/**
 * MVP display-only at-home surcharge (US-002). SPEC: at-home adds an extra charge.
 * US-012 will resolve service prices from `PriceEntry`; this surcharge stays until
 * product merges that logic into the API.
 */
export const AT_HOME_DISPLAY_SURCHARGE_EUR = 12.5
