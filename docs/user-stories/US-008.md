# US-008 - Admin dashboard: bookings list

## Status
- [ ] To do
- [ ] In progress
- [x] Done

## Milestone
- Target: MVP back office
- Depends on: US-007, US-004

## Context
Always align with `docs/SPEC.md`. If anything in this story contradicts or is missing from the spec, flag it before proceeding.

## Story
As the hairdresser, I want to see upcoming and past bookings in a list so that I can plan my day.

## Requirements
- **Admin API:** e.g. `GET /api/admin/bookings?upcoming=true` with pagination/sort by datetime.
- Flesh out `frontend/src/views/admin/AdminDashboardView.vue` to list rows with client contact + service + slot time + status.

## Acceptance Criteria
- [x] Pytest: auth required; returns seeded bookings.
- [x] Vitest: renders sections or sort order for upcoming vs past. (`AdminDashboardView.spec.ts`)

## Out of scope
- Calendar grid (US-009).
