# US-012 - Service pricing by datetime range (`PriceEntry`)

## Status
- [ ] To do
- [ ] In progress
- [x] Done

## Milestone
- Target: MVP back office + pricing correctness
- Depends on: US-007, US-002

## Context
Always align with `docs/SPEC.md`. If anything in this story contradicts or is missing from the spec, flag it before proceeding.

## Story
As the hairdresser, I want to set prices per service for date ranges so that weekends or seasons cost differently.

## Requirements
- **SPEC “Pricing”:** Implement `PriceEntry` linked to `service_id`, `valid_from`, `valid_to`, `price`.
- **Admin APIs** to manage entries (overlap rules: document — e.g. reject overlaps).
- **Public/booking path:** Resolve active price at “now” or at **slot datetime** — **pick and document** (booking-at-slot-date is usually right for salons).
- Update US-002 display and US-004 snapshot to use resolved price (may refactor away hardcoded at-home surcharge into DB constants if desired).

## Acceptance Criteria
- [x] Pytest: resolution picks correct row; booking snapshot stores resolved number. *(Price resolved at slot `datetime.combine(date, start_time)`; `GET /public/slots` uses min across services.)*
- [x] Vitest: admin price entry list + create. (`AdminPricingView.spec.ts`)

## Out of scope
- Rule engine beyond interval rows (explicit in SPEC).
