# US-009 - Admin calendar: slots and bookings

## Status
- [ ] To do
- [ ] In progress
- [x] Done

## Milestone
- Target: MVP back office
- Depends on: US-007, US-001

## Context
Always align with `docs/SPEC.md`. If anything in this story contradicts or is missing from the spec, flag it before proceeding.

## Story
As the hairdresser, I want a calendar view of slots and bookings so that I see the week at a glance.

## Requirements
- **Admin API:** aggregated week data (slots + booking overlays), or reuse public slots + admin bookings merge on frontend — choose one, keep payload bounded.
- New Vue view or extend dashboard with week navigation consistent with client ISO week helper `frontend/src/utils/isoWeek.ts`.

## Acceptance Criteria
- [x] Pytest: week endpoint respects auth.
- [x] Vitest: week navigation + booked slot styling. (`AdminCalendarView.spec.ts`)

## Out of scope
- Drag-drop reschedule (unless trivial — prefer out).
